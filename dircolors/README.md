originally from https://github.com/seebi/dircolors-solarized

usage:
 
    eval `dircolors /path/to/dircolorsdb` > $HOME/.dircolors

and then:

    source $HOME/.dircolors

in shell config, i.e. .bashrc, .zshrc, etc

