# vim:fileencoding=utf-8:ft=conf

font_family Hasklug Nerd Font Mono
font_size        12.0
italic_font      auto
bold_font        auto
bold_italic_font auto

shell_integration no-cursor
cursor_shape block
cursor_blink_interval 0

enable_audio_bell no
remember_window_size  no

# large window
#initial_window_width  241c
#initial_window_height 84c

# smaller window like a 1/4
# of the large screen
initial_window_width  185c
initial_window_height 46c

url_style curly
open_url_modifiers cmd

# --- Mac OS specific settings
editor /opt/homebrew/bin/nvim
macos_option_as_alt yes
macos_quit_when_last_window_closed yes

# hide_window_decorations titlebar-only
# macos_hide_titlebar yes

placement_strategy center
window_padding_width 10

map cmd+c copy_to_clipboard
map cmd+v paste_from_clipboard
mouse_map left click grabbed,ungrabbed mouse_discard_event
mouse_map cmd+left click grabbed,ungrabbed mouse_click_url

# themes are managed using themes kitten
# so all themes should be put into themes folder
# theme switch is done using:
#  kitty +kitten themes [options] [theme name to switch to]
# reference https://sw.kovidgoyal.net/kitty/kittens/themes/

# BEGIN_KITTY_THEME
# Rosé Pine Dawn
include current-theme.conf
# END_KITTY_THEME
